<idea-plugin>
    <id>com.hdl.verilog-vivado-plugin</id>
    <name>HDL and Vivado + MCP Support</name>
    <vendor email="tanawin1701d@outlook.com" url="https://github.com/Merge-Panic-Society/JetBrain-Verilog-Vivado-Plugins">Merge Panic Society</vendor>

    <description><![CDATA[
    <p><b>Let an AI assistant drive Vivado while you watch &mdash; and work alongside it.</b></p>

    <p>FPGA work means living in two windows: your editor for the RTL, and Vivado for everything
    else. This plugin closes that gap, and then goes further &mdash; it can hand your live Vivado
    session to an AI assistant, so building a block design or chasing a timing failure becomes a
    conversation instead of an afternoon of clicking.</p>

    <p>The part that makes it usable: <b>the Vivado GUI stays open and you keep using it</b>. You
    watch the assistant wire up the block design in the canvas as it happens, and you can click,
    inspect and type your own Tcl in the same session while it works. Human in the loop, not a
    black box that hands you a result.</p>

    <h3>Everything it does, at a glance</h3>
    <ul>
        <li><b>AI assistant (MCP), with you in the loop</b> &mdash; let Claude Code, Junie or Codex
            drive your live Vivado session while the <b>GUI stays open in front of you</b>. Watch
            each block design get wired and each run kick off as it happens, and keep clicking and
            typing in the same session while the assistant works. Off by default.</li>
        <li><b>Vivado Console</b> &mdash; a live Tcl terminal on a running session; the Vivado GUI
            stays usable while you type.</li>
        <li><b>Session recording</b> &mdash; one button writes everything sent to and received from
            Vivado into a log file of your choosing, yours and the assistant's alike, with a
            blinking red dot while it runs.</li>
        <li><b>Verilog &amp; SystemVerilog</b> &mdash; highlighting, completion, commenting, and
            <code>module</code>/<code>endmodule</code>-aware brace matching.</li>
        <li><b>Tcl &amp; XDC</b> &mdash; highlighting, folding, <code>${var}</code> handling, and
            Vivado command completion.</li>
        <li><b>Cross-module linting</b> &mdash; Icarus Verilog or Verilator inline, analysing your
            whole design so cross-file instantiations resolve.</li>
        <li><b>One-click Vivado</b> &mdash; build a project from a folder, open an <code>.xpr</code>,
            run Tcl/XDC scripts, package IP.</li>
        <li><b>Vitis &amp; Vitis HLS</b> &mdash; open a workspace or create an HLS kernel from a folder.</li>
    </ul>

    <br>
    <p><b>&mdash;&mdash;&mdash; In detail &mdash;&mdash;&mdash;</b></p>

    <h3>AI assistants can drive your Vivado session</h3>

    <p>Connect <b>Claude Code</b>, <b>Junie</b>, <b>Codex</b>, or any client that speaks the Model
    Context Protocol, and it gets 27 curated, parameter-checked Vivado commands &mdash; the same
    ones you have. It can create and open projects, add sources, set the top module, build and wire
    block designs, apply board presets, run connection automation, assign addresses, generate HDL
    wrappers, run synthesis and implementation, produce a bitstream, and program a device.</p>

    <p>Ask for what you want:</p>
    <ul>
        <li><i>"Create a Zynq project for my KV260, add the PS, and wire up an AXI GPIO."</i></li>
        <li><i>"Synthesis failed &mdash; read the log and tell me what broke."</i></li>
        <li><i>"Add these RTL files, set top to <code>design_1_wrapper</code>, and build a bitstream."</i></li>
    </ul>

    <h3>This is not a headless robot &mdash; you are sitting next to it</h3>

    <p>The assistant drives the <b>same Vivado session you are looking at</b>. The GUI is open the
    whole time, so you see the block design get wired up, the IP get configured and the runs start
    &mdash; live, in the canvas, not as a summary afterwards.</p>

    <p>And you are not locked out while it works. <b>You can keep using Vivado alongside the
    assistant</b>: click around the block design, open a report, or type your own Tcl into the
    Vivado Console. Every command &mdash; yours and the assistant's &mdash; goes through one queue,
    so the two of you share the session without colliding. If you do not like where it is heading,
    you can act on it immediately rather than waiting for it to finish.</p>

    <p><b>You stay in charge the whole time:</b></p>
    <ul>
        <li><b>You start it, every time.</b> The server never starts on its own, and a safety
            agreement explains what you are enabling before it opens.</li>
        <li><b>Arbitrary Tcl is opt-in.</b> The unrestricted-Tcl tools are hidden from the
            assistant and refused unless you enable them for that one session.</li>
        <li><b>Locked down.</b> Loopback only, no CORS headers, browser origins and rebound host
            names refused, and a fresh bearer token minted for every session.</li>
        <li><b>Nothing happens silently.</b> Every command is printed to the Vivado Console
            before it executes, so you watch the work as it happens &mdash; and you can type into
            the same session while the assistant works.</li>
        <li><b>Vivado must be running.</b> No live session means every tool is refused.</li>
    </ul>

    <h3>Vivado Console &mdash; the terminal underneath it</h3>
    <p>The same channel the assistant uses is yours to type into: a bidirectional Tcl terminal
    attached to a live Vivado session. Because it talks over a socket rather than stdin, the Vivado
    GUI stays fully usable while you work. 27 parameterised commands are available from a toolbar
    palette, with command history on the up/down arrows.</p>

    <p><b>Record the session.</b> Press Record, choose where the file goes, and every command and
    every line of output from that point on is written to it &mdash; each record timestamped and
    marked by direction, so you can hand the log to a colleague or grep it for the commands that
    ran. It captures what an AI assistant sends too, not just what you type, which makes it the
    audit trail for a session you let an assistant drive. A red dot blinks on the button while
    recording, and the recording keeps going if you close the tool window.</p>

    <h3>Language support</h3>
    <ul>
        <li><b>Verilog and SystemVerilog</b> (<code>.v .vh .sv .svh</code>) &mdash; syntax
            highlighting, context-aware completion, commenting, and brace matching that pairs
            <code>module</code>/<code>endmodule</code>, <code>begin</code>/<code>end</code>,
            <code>case</code>/<code>endcase</code> and the rest.</li>
        <li><b>Tcl and XDC</b> (<code>.tcl .xdc</code>) &mdash; highlighting, code folding for
            <code>proc</code> bodies and braced blocks, <code>${var}</code> handling, and completion
            for common Vivado commands.</li>
    </ul>

    <h3>Linting that understands your whole design</h3>
    <ul>
        <li>Real-time errors and warnings from <b>Icarus Verilog</b> or <b>Verilator</b>, inline in
            the editor.</li>
        <li>Mark a folder as the <b>Top Folder</b> and the linter analyses your modules together
            &mdash; cross-file instantiations resolve instead of showing up as phantom errors.</li>
        <li>A debugger panel with the raw tool output when you need to see exactly what ran.</li>
    </ul>

    <h3>Vivado and Vitis</h3>
    <ul>
        <li><b>Build a project</b> from a folder of HDL, or open an existing <code>.xpr</code>.</li>
        <li><b>Run a Tcl or XDC script</b> from the project view (<code>Ctrl+Alt+R</code>).</li>
        <li><b>Package IP</b> through IP Composer, and open <b>Vitis</b> workspaces or create
            <b>Vitis HLS</b> kernels.</li>
    </ul>

    <h3>Requirements</h3>
    <ul>
        <li>IntelliJ IDEA 2025.1 or newer.</li>
        <li>Xilinx Vivado (2023.2 or newer recommended) for the Vivado features &mdash; set the
            path in <b>Settings &rarr; Tools &rarr; HDL Settings</b>.</li>
        <li>Icarus Verilog or Verilator for linting. Everything else works without them.</li>
    </ul>

    <p><a href="https://github.com/Merge-Panic-Society/JetBrain-Verilog-Vivado-Plugins">Source,
    issue tracker and documentation on GitHub</a>.</p>

    <p><small>Parts of this plugin were developed with AI assistance. Every release is reviewed and
    covered by an automated test suite; please report anything that looks wrong.<br>
    Not affiliated with, endorsed by, or sponsored by AMD/Xilinx or JetBrains. Vivado, Vitis and
    Xilinx are trademarks of Advanced Micro Devices, Inc. IntelliJ IDEA is a trademark of
    JetBrains s.r.o.</small></p>
  ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.lang</depends>

    <extensions defaultExtensionNs="com.intellij">
        <!-- Verilog Language Support -->
        <fileType name="Verilog"
                  implementationClass="com.hdl.verilog.VerilogFileType"
                  fieldName="INSTANCE"
                  language="Verilog"
                  extensions="v;vh;sv;svh"/>

        <lang.parserDefinition language="Verilog"
                               implementationClass="com.hdl.verilog.VerilogParserDefinition"/>

        <lang.syntaxHighlighterFactory language="Verilog"
                                       implementationClass="com.hdl.verilog.VerilogSyntaxHighlighterFactory"/>

        <lang.commenter language="Verilog"
                       implementationClass="com.hdl.verilog.VerilogCommenter"/>

        <lang.braceMatcher language="Verilog"
                          implementationClass="com.hdl.verilog.VerilogBraceMatcher"/>

        <completion.contributor language="Verilog"
                               implementationClass="com.hdl.verilog.VerilogCompletionContributor"/>

        <!-- Tcl Language Support -->
        <fileType name="Tcl"
                  implementationClass="com.hdl.tcl.TclFileType"
                  fieldName="INSTANCE"
                  language="Tcl"
                  extensions="tcl;xdc"/>

        <lang.parserDefinition language="Tcl"
                               implementationClass="com.hdl.tcl.TclParserDefinition"/>

        <lang.syntaxHighlighterFactory language="Tcl"
                                       implementationClass="com.hdl.tcl.TclSyntaxHighlighterFactory"/>

        <completion.contributor language="Tcl"
                               implementationClass="com.hdl.tcl.TclCompletionContributor"/>

        <colorSettingsPage implementation="com.hdl.verilog.VerilogColorSettingsPage"/>
        <colorSettingsPage implementation="com.hdl.tcl.TclColorSettingsPage"/>

        <lang.foldingBuilder language="Tcl"
                            implementationClass="com.hdl.tcl.TclFoldingBuilder"/>

        <!-- External Annotator for Linting -->
        <externalAnnotator language="Verilog"
                          implementationClass="com.hdl.verilog.linter.VerilogExternalAnnotator"/>

        <!-- Project view decoration for Top Folder -->
        <projectViewNodeDecorator implementation="com.hdl.verilog.TopFolderProjectViewDecorator"/>

        <!-- Settings -->
        <projectConfigurable parentId="tools"
                           instance="com.hdl.settings.HdlSettingsConfigurable"
                           id="com.hdl.settings.HdlSettingsConfigurable"
                           displayName="HDL Settings"/>

        <!-- Only plain classes are declared here. LinterSettingsBroadcaster,
             TclBridgeService, VivadoProcessManager and VivaMcpServer carry
             @Service(Service.Level.PROJECT), which registers them on its own —
             declaring them again is a duplicate registration and an error. -->
        <projectService serviceImplementation="com.hdl.vivado.VivadoSettingsState"/>
        <projectService serviceImplementation="com.hdl.verilog.linter.LinterSettingsState"/>

        <!-- Notification Group -->
        <notificationGroup id="Vivado" displayType="BALLOON"/>

        <!-- Tool Windows -->
        <toolWindow id="HDL Settings"
                    anchor="right"
                    factoryClass="com.hdl.settings.HdlSettingsToolWindowFactory"
                    icon="com.hdl.verilog.VerilogIcons.HDL_PANEL"/>

        <toolWindow id="Verilog Linter Debugger"
                    anchor="bottom"
                    factoryClass="com.hdl.verilog.linter.LinterDebuggerToolWindowFactory"
                    icon="com.hdl.verilog.VerilogIcons.HDL_PANEL"/>

        <toolWindow id="Vivado Console"
                    anchor="bottom"
                    factoryClass="com.hdl.coterm.VivaCoTermToolWindowFactory"
                    icon="com.hdl.verilog.VerilogIcons.HDL_PANEL"/>
    </extensions>

    <actions>
        <!-- Vivado Actions -->
        <group id="VivadoActionGroup"
               class="com.hdl.vivado.actions.VivadoActionGroup"
               text="Vivado"
               description="Vivado operations"
               popup="true">
            <add-to-group group-id="ProjectViewPopupMenu" anchor="last"/>
            <add-to-group group-id="EditorPopupMenu" anchor="last"/>
            <action id="com.hdl.vivado.actions.RunTclScriptAction"
                    class="com.hdl.vivado.actions.RunTclScriptAction"
                    text="Run Tcl Script"
                    description="Run the selected Tcl script in Vivado">
                <keyboard-shortcut first-keystroke="control alt R" keymap="$default"/>
            </action>
            <action id="com.hdl.vivado.actions.IPComposerAction"
                    class="com.hdl.vivado.actions.IPComposerAction"
                    text="IP Composer"
                    description="Launch Vivado IP Composer"/>
            <action id="com.hdl.vivado.actions.BuildProjectAction"
                    class="com.hdl.vivado.actions.BuildProjectAction"
                    text="Build Project"
                    description="Create a Vivado project from HDL files"/>
            <action id="com.hdl.vivado.actions.OpenProjectAction"
                    class="com.hdl.vivado.actions.OpenProjectAction"
                    text="Open Project"
                    description="Open an existing Vivado project"/>
            <separator/>
            <action id="com.hdl.vivado.actions.OpenVitisAction"
                    class="com.hdl.vivado.actions.OpenVitisAction"
                    text="Open Vitis"
                    description="Open selected folder as Vitis workspace"/>
            <action id="com.hdl.vivado.actions.CreateHlsAction"
                    class="com.hdl.vivado.actions.CreateHlsAction"
                    text="Create HLS Kernel"
                    description="Create a new Vitis HLS project"/>
            <separator/>
            <action id="com.hdl.vivado.actions.LaunchVivaCoTermAction"
                    class="com.hdl.vivado.actions.LaunchVivaCoTermAction"
                    text="Launch Vivado Console"
                    description="Open the Vivado Console (Viva-CoTerm) and launch a live Vivado session"/>
        </group>

        <!-- Linter Context Actions -->
        <action id="com.hdl.verilog.SelectTopFolderAction"
                class="com.hdl.verilog.actions.SelectTopFolderAction"
                text="Set as Verilog Top Folder"
                description="Set this folder as the top folder for linting">
            <add-to-group group-id="ProjectViewPopupMenu" anchor="after" relative-to-action="VivadoActionGroup"/>
        </action>
    </actions>
</idea-plugin>
